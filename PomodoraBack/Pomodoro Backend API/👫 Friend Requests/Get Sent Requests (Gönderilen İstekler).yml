info:
  name: Get Sent Requests (Gönderilen İstekler)
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/friendrequest/sent"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Gönderilen beklemede arkadaş isteklerini getir
