info:
  name: Get All Users (Tüm Kullanıcıları Listele)
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Tüm kullanıcıları listele (Auth gerekli değil)
