info:
  name: Remove Friend (Arkadaş Kaldır)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/api/friendship/{{friend_id}}"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Arkadaşlık ilişkisini kaldır
